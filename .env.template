# Google Gemini API Configuration
# Get your API key from: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY="your_gemini_api_key_here"

# Docker Container Configuration
# IMPORTANT: Check your actual container name with: docker ps
#
# You're using the MCP Docker Gateway (docker mcp gateway run)
# Common container names:
#   - "mcp-gateway"     (typical for gateway setups)
#   - "mcp-toolkit"     (if using full toolkit)
#   - Custom name you may have set
#
# The gateway provides access to multiple MCP servers:
#   - notion, github-official, playwright, perplexity-ask
MCP_CONTAINER_NAME="mcp-gateway"
